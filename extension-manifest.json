{
  "id": "prune-context",
  "name": "Context Prune + Summary Engine",
  "version": "3.0.0",
  "description": "Sidecar summarization engine: auto-compresses tool call outputs into summaries, projects summaries into message stream, triggers global collapse at high context usage. Session-aware summary persistence across branches.",
  "tier": "community",
  "requires": {
    "platform": ">=2.29.0"
  },
  "provides": {
    "hooks": ["session_start", "context", "turn_end", "input"]
  }
}
}
